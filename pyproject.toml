[project]
name = "matmuls"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jax[cuda12-local]>=0.7.2",
]

[dependency-groups]
dev = [
    "gpustat>=1.1.1",
    "nvitop>=1.5.3",
]
